require "spec_helper"

RSpec.describe Parliament::Opensearch do
  it "has a version number" do
    expect(Parliament::Opensearch::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
